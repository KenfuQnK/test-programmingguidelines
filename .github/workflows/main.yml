name: Build and Deploy
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    if: ${{ github.repository == 'KenfuQnK/test-programmingguidelines' }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install and Build
        run: |
          npm ci
          npx quartz build

      # Aquí puedes agregar los pasos para desplegar tu sitio, si es necesario.
